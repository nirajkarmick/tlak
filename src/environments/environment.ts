// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  //ip:"https://xm.tlakapp.com/api/v7",
  ip: "https://account.tlakapp.com/tlak/api/v7",
  pullit: "https://api.tutterflycrm.com/ptprog/api/",
  pullit_web: "https://api.tutterflycrm.com/ptprog/",
  coordinateKey:'AIzaSyDe20rB3ZDxDKMs4NTje6ueQTTtp1JixVo',
  mode: 'Dev'
};

//https://account.tlakapp.com/tlakvxm/index.php/api/v7/login

// api.tutterflycrm.com/ptprog/api/search-pullit-poi

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
